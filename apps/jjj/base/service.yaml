apiVersion: v1
kind: Service
metadata:
  name: jjj
  namespace: default
spec:
  selector:
    app: jjj
  # type: LoadBalancer | ClusterIP | NodePort
  ports:
    - name: service-port
      port: 8081 # service port
      targetPort: 80 # container port
