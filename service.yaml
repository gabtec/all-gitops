apiVersion: v1
kind: Service
metadata:
  name: fbs
  namespace: 
spec:
  selector:
    app: fbs
  # type: LoadBalancer | ClusterIP | NodePort
  ports:
    - name: http
      port: 8081 # service port
      protocol: TCP
      targetPort: 80 # container port
