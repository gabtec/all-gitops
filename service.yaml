apiVersion: v1
kind: Service
metadata:
  name: mmm
  namespace: 
spec:
  selector:
    app: mmm
  # type: LoadBalancer | ClusterIP | NodePort
  ports:
    - name: http
      port: 8081 # service port
      protocol: TCP
      targetPort: 80 # container port
